Hero images are intentionally client-uploaded or text-only by default.

The app avoids bundled clipart/cartoon vehicle assets. Add real dealership,
showroom, service-lane, or vehicle photography here later and register those
paths in `lib/hero-library.ts`.
